id: coding_003
category: coding
prompt: "Write Python code defining slugify(text: str) -> str that lowercases text, replaces runs of non-alphanumeric characters with one hyphen, and trims hyphens. Return only the code."
description: "String normalization implementation."
expected_outcomes: ["slugify", "lowercase", "hyphen"]
expected_tool_calls:
  - tool: code_exec
    required: false
    min_calls: 0
    max_calls: 3
allowed_tools: ["code_exec"]
hidden_tests: test_slugify.py
output_format: code
scoring:
  weights: {coding_tests: 0.7, completion: 0.2, tool: 0.1}
  pass_threshold: 0.8
timeout_s: 120
max_tool_calls: 4
